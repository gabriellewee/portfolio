---
title: Front-end Developer, Designer, and Creative
description: I’m a front‑end developer working at Apple .

layout: layouts/base.njk
key: home
style: true
links: true
add: true
tags: pages

pagination:
    data: collections.entries
    size: 10
    reverse: true
---

<section class="posts-container">
	<div class="posts grid-isotope">
		{% for post in pagination.items %}
			<article class="post {% if (post.data.pagination.alias) and (post.data.pagination.alias != post.data.category) %} post-{{post.data.category}} post-{{post.data.pagination.alias}}{% else %} post-{{post.data.category}}{% endif %}{% if (post.data.small == true) or (post.data.category == "social") %} post-small{% endif %}{% if post.data.category == "media" %} post-{{post.data.pagination.data}}{% endif %} grid-item"{% if post.data.category == "media" %} data-orientation="{% stats './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, 'orientation' %}"{% endif %}>
				{% if post.data.category == "media" %}
					<input class="{{post.data.category}}-trigger" id="{{post.data.date | safe}}-{{post.data.name}}-info" type="checkbox" tabindex="0" aria-label="Show information about this {{post.data.type}}" role="switch" aria-checked="false"/>
				{% endif %}
				<div class="post-wrapper">
					<header class="post-header{% if (post.data.category == "writing") or (post.data.category == "code") %} inline-header{% endif %}">
						{% if post.data.category == "writing" %}
							{% if post.data.title %}
								<h2>
									<a href="{{post.url | url}}">{{post.data.title | safe}}</a>
								</h2>
							{% endif %}
						{% elseif post.data.category == "code" %}
							{% if post.data.title %}
								<h2>
									<a href="https://codepen.io/gabriellewee/pen/{{post.data.uri}}" target="_blank">
										<span>{{post.data.title | safe}}</span>
									</a>
								</h2>
							{% endif %}
						{% elseif post.data.category == "social" %}
							<a href="{{post.data.url}}" target="_blank">
								{% include 'svg/logos/' + post.data.pagination.alias + '.svg' %}
							</a>
						{% endif %}
						{% if post.data.category != "media" %}
							<time{% if post.data.category != "code" %} class="time-default"{% endif %} datetime="{{post.page.date | htmlDateString}}" title="{{post.page.date | readableDate}}">{{post.page.date | timeAgo}}</time>
						{% endif %}
					</header>
					<div class="post-content">
						{% if post.data.category == "media" %}
							<figure role="group" aria-label="{{post.data.type | capitalize}}{% if post.data.location %}, {{post.data.location}}{% elseif post.data.title %}, {{post.data.title}}{% endif %}" style="aspect-ratio: {% stats './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, 'ratio' %};">
								{% if post.data.upscale %}
									<picture>
										<img class="upscale" src="{% include post.data.upscale %}"{% if post.data.upscaleWidth %} width="{{post.data.upscaleWidth}}"{% endif %}{% if post.data.upscaleHeight %} height="{{post.data.upscaleHeight}}"{% endif %} alt="{{post.data.description}}"/>
									</picture>
								{% else %}
									{% image './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, post.data.description, 'default', loop.index %}
								{% endif %}
								<figcaption class="{{post.data.category}}-data">
									<time class="time-default" datetime="{{post.page.date | htmlDateString}}" title="{{post.page.date | readableDate}}">{{post.page.date | timeAgo}}</time>
									<label class="media-down media-icon" for="{{post.data.date | safe}}-{{post.data.name}}-info"></label>
									<div class="media-thumbnail">
										<a class="media-expand expand" href="#{{post.data.date | safe}}-{{post.data.name}}-lightbox" aria-label="Expand {{post.data.type}}" tabindex="-1" role="button">
											<span class="no-image">Expand</span>
											{% if post.data.upscale %}
												<picture>
													<img class="upscale" src="{% include post.data.upscale %}"{% if post.data.upscaleWidth %} width="{{post.data.upscaleWidth}}"{% endif %}{% if post.data.upscaleHeight %} height="{{post.data.upscaleHeight}}"{% endif %} alt="{{post.data.description}}"/>
												</picture>
											{% else %}
												{% image './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, post.data.description, 'thumbnail', loop.index %}
											{% endif %}
										</a>
									</div>
									<div class="media-links">
										{% for link in post.data.external %}
											<a class="media-link external {{link.platform}}" target="_blank" href="{{link.url}}" tabindex="-1" aria-label="View on {{link.platform | capitalize}}"><span>{{link.platform | capitalize}}</span></a>
										{% endfor %}
									</div>
									<dl class="media-meta">
										{% if post.data.title %}
											<div class="meta-title">
												<dt>Title</dt><dd>{{post.data.title | nbsp | safe}}</dd>
											</div>
										{% endif %}
										{% if post.data.location %}
											<div class="meta-location">
												<dt>Location</dt><dd>{{post.data.location}}</dd>
											</div>
										{% endif %}
										{% if post.data.camera %}
											<div class="meta-camera">
												<dt>Camera</dt><dd>{{post.data.camera}}</dd>
											</div>
										{% endif %}
										{% if post.data.editor %}
											<div class="meta-editor">
												<dt>Editor</dt><dd>{{post.data.editor}}</dd>
											</div>
										{% endif %}
										{% if post.data.tools %}
											<div class="meta-tools">
												<dt>Tools</dt><dd>{{post.data.tools}}</dd>
											</div>
										{% endif %}
									</dl>
								</figcaption>
							</figure>
						{% elseif post.data.category == "code" %}
							<iframe src="{{post.url | url}}" width="100%" height="{{post.data.height}}" title="{{post.data.title | safe}}"></iframe>
						{% elseif post.data.content %}
							{{post.data.content | safe}}
						{% else %}
							{% excerpt post %}
						{% endif %}
					</div>
				</div>
			</article>
		{% endfor %}
	</div>
	<nav class="pagination main-pagination">
		<button class="load"><span><span>Load more</span></span></button>
		{% if pagination.href.previous %}
			<a class="previous" href="{{ pagination.href.previous }}"><span><span>Previous</span></span></a>
		{% endif %}
		{% if pagination.href.next %}
			<a class="next" href="{{ pagination.href.next }}"><span><span>Next</span></span></a>
		{% endif %}
	</nav>
</section>

<div class="lightbox-group posts-lightbox-group" hidden>
	{% for post in pagination.items %}
		{% if post.data.category == "media" %}
			<a class="lightbox post-lightbox" role="dialog" aria-label="Modal" aria-modal="true" id="{{post.data.date | safe}}-{{post.data.name}}-lightbox" href="#{{post.data.date | safe}}-{{post.data.name}}">
				<figure class="image">
					{% if post.data.upscale %}
						<picture>
							<img class="upscale" src="{% include post.data.upscale %}"{% if post.data.upscaleWidth %} width="{{post.data.upscaleWidth}}"{% endif %}{% if post.data.upscaleHeight %} height="{{post.data.upscaleHeight}}"{% endif %} alt="{{post.data.description}}"/>
						</picture>
					{% else %}
						{% image './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, post.data.description, 'screen' %}
					{% endif %}
				</figure>
			</a>
		{% endif %}
	{% endfor %}
	<div class="lightbox-background post-lightbox"></div>
</div>