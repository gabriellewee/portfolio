---
title: Front-end Developer, Designer, and Creative
description: I’m a front‑end developer working at Apple .

layout: layouts/base.njk
key: home
style: true
links: true
add: true
tags: pages

pagination:
    data: collections.posts
    size: 10
    reverse: true
---

<section class="posts grid-isotope">
	{% for post in pagination.items %}
		<article class="post {% if (post.data.pagination.alias) and (post.data.pagination.alias != post.data.category) %} post-{{post.data.category}} post-{{post.data.pagination.alias}}{% else %} post-{{post.data.category}}{% endif %}{% if post.data.short == true %} post-short{% endif %} grid-item"{% if post.data.category == "media" %} data-orientation="{% stats './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, 'orientation' %}"{% endif %}>
			<header class="post-header">
				{% if post.data.category == "writing" %}
					{% if post.data.title %}
						<h2><a href="{{post.url | url}}">{{post.data.title | safe}}</a></h2>
					{% endif %}
				{% elseif post.data.category == "code" %}
					{% if post.data.title %}
						<h2>
							<a href="https://codepen.io/gabriellewee/pen/{{post.data.uri}}" target="_blank"><span>{{post.data.title | safe}}</span></a>
						</h2>
					{% endif %}
				{% elseif post.data.category == "social" %}
					<a href="{{post.data.url}}" target="_blank">
						{% include 'svg/logos/' + post.data.pagination.alias + '.svg' %}
					</a>
				{% endif %}
				<time class="time" datetime="{{post.page.date | htmlDateString}}">{{post.page.date | readableDate}}</time>
			</header>
			<div class="post-content">
				{% if post.data.category == "media" %}
					{% if post.data.upscale %}
						<picture>
							<img class="upscale" src="{% include post.data.upscale %}"{% if post.data.upscaleWidth %} width="{{post.data.upscaleWidth}}"{% endif %}{% if post.data.upscaleHeight %} height="{{post.data.upscaleHeight}}"{% endif %} alt="{{post.data.description}}" style="image-rendering: pixelated;"/>
						</picture>
					{% else %}
						{% image './static/images/' + post.data.pagination.data + '/' + post.data.date + '-' + post.data.name + '.' + post.data.extension, post.data.description, 'default', loop.index %}
					{% endif %}
				{% elseif post.data.category == "code" %}
					<iframe src="{{post.url | url}}" width="100%" height="{{post.data.height}}"></iframe>
				{% elseif post.data.content %}
					{{post.data.content | safe}}
				{% else %}
					{% excerpt post %}
				{% endif %}
			</div>
		</article>
	{% endfor %}
</section>

<nav class="pagination">
	<button class="load">Load more</button>
	{% if pagination.href.previous %}
		<a class="previous" href="{{ pagination.href.previous }}"><span>Previous</span></a>
	{% endif %}
	{% if pagination.href.next %}
		<a class="next" href="{{ pagination.href.next }}"><span>Next</span></a>
	{% endif %}
</nav>