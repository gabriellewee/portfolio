---
title: Archive · Writing
description: Gabrielle Wee’s personal blog and photo journal archive.

permalink: /writes/archive/
layout: layouts/base.njk
key: writing
og: true
style: true
tags: subpages
---

{% set showYear = true %}
{% set currentYear = "" %}

<article class="archive">
	<ul>
		{% for post in collections.writes | reverse %}
			{% if showYear == true %}
				{% set postYear = post.date.getFullYear() %}
				{% if currentYear != postYear %}
					{% set currentYear = postYear %}
					<li class="year">
						<h2>{{ postYear }}</h2>
					</li>
				{% endif %}
			{% endif %}
			<li>
				<h3><a href="{{post.url | url}}">{{post.data.title}}</a></h3>
				<span role="text"><time datetime="{{post.date | htmlDateString}}">{{post.date|readableDate}}</time></span>
			</li>
		{% endfor %}
	</ul>
</article>